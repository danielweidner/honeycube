namespace HoneyCube.Editor.Events
{
    /// <summary>
    /// An event that is raised every time the user has closes the application 
    /// log. Allows event handler to perform certain action on closing.
    /// </summary>
    public class AppLogClosingEvent
    {
        // Empty
    }
}
